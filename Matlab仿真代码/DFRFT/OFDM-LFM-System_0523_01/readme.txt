该系统：是完好的系统，可以直接运行。
完成的功能：1. 整体就是三种情况下的性能对比：性能两个为PAPR和BER。
			2. 两个图像：A.PAPR B.BER
			3. 三种情况：传统的OFDM-LFM系统；采用SLM算法的OFDM-LFM系统；采用SLM算法的OFDM-LFM系统；
			自己提出的优化的SLM算法的OFDM-LFM

自由优化算法：
	 分析PAPR抑制算法的改进；在进行SLM或PTS算法之前进行PAPR判断。
		若PAPR大于阈值，则进行SLM模块；
		若PAPR小于阈值，直接通过，不进行调整。		
		将附加信息也加到调制前的数据中；
		
		
		验证系统的正确与否：
		在不添加噪声的情况下，观察下误码率是不是为0.
		
		CTRL + R : 注释快捷键；
		CTRL + T : 取消注释快捷键。